# Scripts

- `validate_project_context.py`: validates required AI continuity files and state JSON consistency.
- `init-git.sh`: initializes the repository on macOS/Linux.
- `init-git.ps1`: initializes the repository on Windows PowerShell.

Application-specific scripts will be added only after the technology stack is approved.
